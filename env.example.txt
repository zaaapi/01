# Variáveis de Ambiente - LIVIA

# Supabase Configuration
# Obtenha essas informações no dashboard do Supabase: https://app.supabase.com
# Settings > API

# Project URL (exemplo: https://abcdefghijklmnop.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co

# Anon Public Key (use a chave "anon" "public", NÃO a "service_role")
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua-chave-anon-key-aqui

# N8N Configuration
# Para integração com automações e envio de mensagens WhatsApp

# Base URL do N8N (exemplo: https://seu-n8n.com/webhook)
NEXT_PUBLIC_N8N_BASE_URL=https://seu-n8n-instance.com/webhook

# JWT Secret para autenticação N8N (NÃO commitar! Apenas em .env.local)
# Exemplo: NdnpQeMzrlvi1TnluSJwSpibok45FfT4
N8N_JWT_SECRET=sua-senha-jwt-aqui

# Instruções Detalhadas:
# 1. Acesse https://app.supabase.com
# 2. Selecione seu projeto
# 3. Vá em Settings > API
# 4. Copie a "Project URL" e cole acima
# 5. Copie a chave "anon" "public" (NÃO use service_role)
# 6. Cole acima substituindo "sua-chave-anon-key-aqui"
# 7. Configure as variáveis do N8N (URL base e senha JWT)
# 8. Salve este arquivo como .env.local na raiz do projeto
# 9. Reinicie o servidor de desenvolvimento (npm run dev)

# IMPORTANTE:
# - NUNCA commite o arquivo .env.local (ele já está no .gitignore)
# - NUNCA use a chave service_role no frontend
# - NUNCA commite a senha N8N_JWT_SECRET
# - Veja docs/SUPABASE_ENV_SETUP.md para guia completo passo a passo
